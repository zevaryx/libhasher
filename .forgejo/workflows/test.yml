on:
  push:
    branches-ignore:
      - main

jobs:
  test:
    runs-on: ubuntu-rust
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: cargo test