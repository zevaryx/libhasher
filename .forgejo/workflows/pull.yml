on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-rust
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: make test